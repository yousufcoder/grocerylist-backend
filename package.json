{
  "name": "grocerylist-backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env PGUSER=postgres PGHOST=localhost PGPASSWORD='root' PGDATABASE=grocery_list PGPORT=5432 nodemon server",
    "start": "node server"
  },
  "author": "mohammed yousuf",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^5.2.0",
    "nodemon": "^1.18.11"
  },
  "dependencies": {
    "express": "^4.16.4",
    "pg": "^7.10.0"
  }
}
